export enum IngredientCategory {
  PROTEIN = 'protein',
  PRODUCE = 'produce',
  PANTRY = 'pantry',
}

export enum IngredientUnit {
  G = 'g',
  ML = 'ml',
  PIECE = 'piece',
  TBSP = 'tbsp',
  TSP = 'tsp',
}
